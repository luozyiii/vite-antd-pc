# 开发环境 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装 git（某些 npm 包可能需要）
RUN apk add --no-cache git

# 复制 package.json 和 package-lock.json
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动开发服务器
CMD ["npm", "run", "dev"]
